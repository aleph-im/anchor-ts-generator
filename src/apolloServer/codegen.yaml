schema: src/apolloServer/schema.graphql
generates:
  output/switchboard_v2/graphql/types.ts:
    plugins:
      - typescript
  output/switchboard_v2/graphql/resolvers.ts:
    plugins:
      - typescript-resolvers


